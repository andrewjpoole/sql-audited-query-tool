# Orchestration Log — Legolas (Frontend Dev)

**Date:** 2026-03-06T13:19Z
**Task:** Add isReadOnly to suggestion serialization (backend + frontend), swap "Insert & Execute" for "Run in Simulator" on write queries
**Requested by:** Andrew
**Status:** SUCCESS

## Summary
Legolas (Frontend) coordinated with backend to conditionally render the correct button based on query type. Backend now sends `isReadOnly` flag with suggestions; frontend uses it to show either "Insert & Execute" (readonly queries) or "🔬 Run in Simulator" (write queries).

## Changes
- **Program.cs:** Added `isReadOnly` serialization in both streaming SSE (`/api/chat` path) and non-streaming response path
- **queryApi.ts:** Extended `QuerySuggestion` interface with optional `isReadOnly?: boolean` field
- **ChatPanel.tsx:** Updated SuggestionCard rendering to check `isReadOnly` and conditionally render correct button

## Outcomes
✅ Backend sends `isReadOnly` flag with all suggestions
✅ Frontend conditionally renders "Insert & Execute" or "Run in Simulator" based on query type
✅ Backward compatibility maintained (undefined isReadOnly defaults to true for safety)
✅ Both npm and dotnet builds passing
✅ No test failures

## Files Modified
- src/SqlAuditedQueryTool.App/Program.cs
- src/SqlAuditedQueryTool.App/ClientApp/src/api/queryApi.ts
- src/SqlAuditedQueryTool.App/ClientApp/src/components/ChatPanel.tsx

## Dependencies
- Coordinator's WriteScriptSimulator removal must complete first (completed ✅)
